import React from 'react';
import './style.scss';

function Header() {
  return (<header className="app-header">
    <div className="container">
      <div className="row">
        <div>React Frontend (logo)</div>
      </div>
    </div>
  </header>);
}

export default Header;
